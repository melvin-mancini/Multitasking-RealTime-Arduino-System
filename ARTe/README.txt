1 - Run the Arduino IDE in the folder work/arduino.exe
2 - Import the library DigitalToggle.zip in the folder Arte through the Arduino IDE.


ARTe works only in the Windows 8.1 platform. In other Windows version, the IDE is not guaranteed. 